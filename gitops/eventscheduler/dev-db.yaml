apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: "scheduler-db"
  namespace: openshift-gitops
spec:
  generators:      
  - clusterDecisionResource:
      configMapRef: acm-placement
      labelSelector:
        matchLabels:
          cluster.open-cluster-management.io/placement: scheduler-dev-placement
      requeueAfterSeconds: 180

  template:
    metadata:
      name: 'scheduler-db-{{name}}'
    spec:
      source:
        repoURL: "https://github.com/rprakashg-redhat/scheduler-ops"
        path: "apps/db/overlays/dev"
        targetRevision: "HEAD"

      destination:
        namespace: "schedule"
        server:  "{{server}}"

      project: default 

  syncPolicy:
    syncOptions:
    - ApplyOutOfSyncOnly=true
    - PruneLast=true
    - CreateNamespace=true
    - Replace=true
      
    automated:
      prune: true
      allowEmpty: true
      selfHeal: true