---
apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: configuration-policies

policyDefaults:
  namespace: "policies"
  
  pruneObjectBehavior: "DeleteIfCreated"

  standards:
  - "NIST SP 800-53"

  categories:
  - "CM Configuration Management"
  
  controls:
  - "CM-2 Baseline Configuration"
  
  complianceType: "musthave"
  
  evaluationInterval:
    compliant: 1m
    noncompliant: 45s
  
  remediationAction: "enforce"
  
  severity: "high"

  placement:
    clusterSelectors:
      environment: "dev"
      vendor: "OpenShift"

policies:    
  - name: install-gatekeeper-operator
    disabled: false
    manifests:
    - path: ./install-gatekeeper-operator

  - name: install-postgresql-operator
    disabled: false
    manifests:
    - path: ./install-postgresql-operator

  - name: container-deny-latest-tag
    categories:
    - setup
    controls: 
    - setup
    disabled: false
    manifests:
    - path: input/container-deny-latest-tag

...
